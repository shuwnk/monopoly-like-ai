# WebSocket URL of the deployed Colyseus server.
# Local dev falls back to ws://localhost:2567 when this is unset.
# In production set it in the Vercel dashboard to your Railway server's URL,
# using the wss:// scheme, e.g.:
VITE_SERVER_URL=wss://your-app.up.railway.app
